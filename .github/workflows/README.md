# GitHub Actions Workflows

GitHub Actions workflow 를 실제로 동작시키려면 workflow 파일을 `.github/workflows` 경로에 위치시켜야 한다.

이 저장소에서는 학습용 예제를 `workflows-study/` 아래에 정리하고, 실습용 예제를 `workflows-practice/` 아래에 정리한다.
하위 디렉토리에 둔 파일은 학습과 정리를 위한 예제이다.
해당 파일을 실제 workflow 실행 대상으로 두려면 `.github/workflows/` 경로에 위치시키면 동작한다.
